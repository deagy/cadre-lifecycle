# The deagy/cadre revision this repository's generated content corresponds to.
#
# Everything under skills/, agents/, codex-agents/, suite/, agent-catalog.json,
# bin/, and plugins/lifecycle/skills/ is produced by that revision's
# `bin/cadre generate-plugin`. plugins/lifecycle/skills/ is the one nested
# exception: lifecycle-onboarding/lifecycle-review generate there instead of
# root skills/ (see SKILL_PACKAGE_TARGETS in the register's
# generate_global_plugin.py) so cadre-lifecycle can package them as part of
# the optional cadre-lifecycle-core plugin. plugins/lifecycle/'s own
# manifests and tools/, and plugins/lifecycle-github/, plugins/lifecycle-
# gitlab/ entirely, are hand-authored here, like README.md below.
# .github/workflows/drift-check.yml re-verifies weekly that generated content
# here still matches what `cadre generate-plugin` produces from the revision
# pinned below, and opens a tracking issue on any diff -- it catches a
# generated file being hand-edited here. It does not catch cadre moving
# forward; regenerate.yml handles that, triggered by cadre's own release.
# Note
# README.md itself is hand-authored here despite being generated content in
# the register's own downstream-distribution template: the register's
# README template (packaging/plugin-README.md, formerly rendered into the
# now-archived deagy/cadre-plugin) describes a different, single-plugin
# repository identity, not this one's merged Cadre + Agentic SDLC + Cline
# identity, so it is deliberately excluded from regeneration.
#
# To pick up register changes: update this ref and commit the regenerated diff
# in the same pull request.
2434ddd874efcb22289dc046f3476edddaaf9d55
