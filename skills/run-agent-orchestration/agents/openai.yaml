interface:
  display_name: "Run Agent Orchestration"
  short_description: "Select and coordinate secure repository agents"
  default_prompt: "Use $run-agent-orchestration to select and coordinate the repository agents needed for this task."
policy:
  allow_implicit_invocation: true
